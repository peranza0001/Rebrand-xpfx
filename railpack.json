{
  "provider": "node",
  "install": "NPM_CONFIG_PRODUCTION=false npm ci --no-audit --no-fund",
  "build": "npm run predeploy -- --skip-env-check && npm run build --workspace=artifacts/api-server && npm run build --workspace=artifacts/nextrade && npm run build --workspace=artifacts/admin-portal",
  "start": "NPM_CONFIG_PRODUCTION=false npm run prepare:runtime-secrets && NODE_ENV=production node --enable-source-maps artifacts/api-server/dist/index.mjs",
  "env": {
    "PORT": "3000"
  }
}
