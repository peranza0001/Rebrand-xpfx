{
  "provider": "node",
  "install": "npm install --legacy-peer-deps --no-audit --no-fund",
  "build": "rm -rf artifacts/nextrade/node_modules/.vite artifacts/admin-portal/node_modules/.vite artifacts/mockup-sandbox/node_modules/.vite && npm run predeploy --skip-env-check && npm run build --workspace=artifacts/api-server",
  "start": "npm run prepare:runtime-secrets && NODE_ENV=production node --enable-source-maps artifacts/api-server/dist/index.mjs",
  "env": {
    "PORT": "3000"
  }
}
