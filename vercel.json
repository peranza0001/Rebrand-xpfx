{
  "buildCommand": "npm install && npm run prepare:runtime-secrets && npm run build --workspace=artifacts/nextrade",
  "outputDirectory": "artifacts/nextrade/dist/public",
  "installCommand": "npm install",
  "framework": null,
  "env": {
    "NODE_ENV": "production"
  }
}
