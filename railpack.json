{
  "provider": "node",
  "install": "npm install --legacy-peer-deps --no-audit --no-fund",
  "build": "npm cache clean --force && sleep 1 && find artifacts -type d -name .vite -exec rm -rf {} + 2>/dev/null || true && npm run predeploy --skip-env-check && npm run build --workspace=artifacts/api-server",
  "start": "npm run prepare:runtime-secrets && NODE_ENV=production node --enable-source-maps artifacts/api-server/dist/index.mjs",
  "env": {
    "PORT": "3000"
  }
}
